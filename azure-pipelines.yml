trigger:
  branches:
    include:
      - main
  pr:
    - '*'

pool:
  vmImage: 'windows-latest'

variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  solution: '../../Avelraangame.Avelraangame.sln'
  project: '../../Avelraangame.Avelraangame.csproj'

steps:
  - task: DotNetCoreCLI@2
    displayName: 'NuGet tool installer'
    inputs:
      command: 'restore'
      restoreSolution: '$(solution)'
      feedToUse: 'select'

  - task: DotNetCoreCLI@2
    displayName: 'Restore and publish NuGet packages'
    inputs:
      command: 'restore'
      projects: '$(solution)'
      feedToUse: 'select'
      additionalArguments: '--publish-output $(Build.ArtifactStagingDirectory)/NUPKGS'

  - task: DotNetCoreCLI@2
    displayName: 'Run tests'
    inputs:
      command: 'test'
      projects: '$(solution)'
      arguments: '--configuration $(BuildConfiguration)'

  - task: DotNetCoreCLI@2
    displayName: 'Publish artifacts'
    inputs:
      command: 'publish'
      publishWebProjects: true
      arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
      zipAfterPublish: true

  - task: PublishBuildArtifacts@1
    displayName: 'Publish artifacts to Azure App Service'
    inputs:
      PathtoPublish: $(Build.ArtifactStagingDirectory)
      ArtifactName: AzureTestProject
      AzureSubscription: Subscription 1
      AzureResourceGroup: Avelraangameresourcegroup
      AzureAppServiceName: Avelraangame
